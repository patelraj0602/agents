# autofix_agent_rules

1. The agent must automatically detect common code issues (formatting, linting errors, missing imports) and apply safe fixes without modifying the intended functionality.

2. The agent must only apply autofixes that are deterministic and reversible, and must avoid making changes when the fix could alter business logic or introduce new behavior.
